<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>TrendPost - Automated Social Media Content from Trending Topics</title>
  <meta name="description" content="Save hours by automating your social media content creation with trending topics customized for your audience.">
  
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- Google Fonts -->
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700&display=swap" rel="stylesheet">

  <!-- Rails built-in CSS bundling -->
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  
  <!-- Rails 8 JS modules with ESM support -->
  <%= javascript_importmap_tags %>
  <%= turbo_include_tags %>

</head>

<body>
  <nav class="navbar">
    <div class="container">
      <a href="/" class="navbar-brand">
        <%= image_tag "logo.svg", alt: "TrendPost Logo", height: 40 %>
      </a>

      <div class="navbar-links">
        <a href="#benefits">Benefits</a>
        <a href="#how-it-works">How It Works</a>
        <a href="#pricing">Pricing</a>
      </div>

      <div class="navbar-actions">
        <% if user_signed_in? %>
          <%= link_to "Logout", destroy_user_session_path, method: :delete, data: { turbo_method: :delete }, class: "btn btn-outline" %>
        <% else %>
          <%= link_to "Login", new_user_session_path, class: "btn btn-outline" %>
          <%= link_to "Sign Up", new_user_registration_path, class: "btn btn-primary", style: "margin-left: 0.5rem;" %>
        <% end %>
      </div>


      <button class="mobile-menu-toggle" aria-label="Toggle menu">
        <span></span>
        <span></span>
        <span></span>
      </button>
    </div>
  </nav>

  <% if flash[:success] %>
    <div class="flash flash-success">
      <%= flash[:success] %>
    </div>
  <% end %>
  
  <% if flash[:error] %>
    <div class="flash flash-error">
      <%= flash[:error] %>
    </div>
  <% end %>

  <main>
    <%= yield %>
  </main>

  <footer class="footer">
    <div class="container">
      <div class="footer-brand" style="text-align: center;">
        <%= image_tag "logo-white.svg", alt: "TrendPost Logo", height: 40 %>
        <p>Automating social media content creation with trending topics customized for your audience.</p>
      </div>
      <div class="footer-bottom" style="text-align: center; margin-top: 2rem;">
        <p>&copy; <%= Date.today.year %> TrendPost. All rights reserved.</p>
      </div>
    </div>
  </footer>
</body>
</html>
